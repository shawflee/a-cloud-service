spring:
  cloud:
    config:
      name: configclient
      lable: master
      discovery:
        enabled: true
        service-id: config-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/